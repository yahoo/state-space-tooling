tests_unit_TESTS = \
  tests/unit/list.test \
  \
  tests/unit/sst.test \
  tests/unit/want.test \
  \
  tests/unit/all.test \
  $(end)

tests_usage_TESTS_DECLARED = $(tests_usage_TESTS)
tests_usage_TESTS_OBSERVED = $(wildcard tests/usage/*.test)
tests_usage_TESTS_ORPHANED = $(filter-out $(tests_usage_TESTS_DECLARED), $(tests_usage_TESTS_OBSERVED))
$(if $(tests_usage_TESTS_ORPHANED), $(error orphaned tests $(tests_usage_TESTS_ORPHANED)))
